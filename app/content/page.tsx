import React from "react";
import Content from "./Content";

type Props = {};

const page = (props: Props) => {
    return (
        <div>
            <Content />
        </div>
    );
};

export default page;
