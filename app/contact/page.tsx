import React from "react";
import Contact from "./Contact";

type Props = {};

const page = (props: Props) => {
    return (
        <div>
            <Contact />
        </div>
    );
};

export default page;
