import React from "react";
import Services from "./Services";

type Props = {};

const page = (props: Props) => {
    return (
        <div>
            <Services />
        </div>
    );
};

export default page;
