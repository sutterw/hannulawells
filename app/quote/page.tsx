import React from "react";
import Quote from "./Quote";

type Props = {};

const page = (props: Props) => {
    return (
        <div>
            <Quote />
        </div>
    );
};

export default page;
