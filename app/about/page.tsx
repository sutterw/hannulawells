import React from "react";
import About from "./About";

type Props = {};

const page = (props: Props) => {
    return (
        <div>
            <About />
        </div>
    );
};

export default page;
